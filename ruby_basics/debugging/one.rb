# 1. Reading Error Messages
# You come across the following code. What errors does it raise for the given examples and what exactly do the error messages mean?

def find_first_nonzero_among(numbers)
  numbers.each do |n|
    return n if n.nonzero?
  end
end

# Examples

find_first_nonzero_among(0, 0, 1, 0, 2, 0)
find_first_nonzero_among(1)

# First method call gives error that too many arguments were passed.
# second method call gives error the #each method can not be applied because the argument passed is not an array.
