# What's the major difference between an Array and a Hash?

Hashes allow labels for data (using Keys) while arrays do not. 
Additionally, an array holds data in a specified order while a hash doesn't necessarily (depending on the version of Ruby).
